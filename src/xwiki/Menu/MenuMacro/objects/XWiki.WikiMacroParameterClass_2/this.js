XWikiObj(function (obj) {
    obj.setDefaultValue("");
    obj.setDescription("Optional menu identifier that will be set on the HTML element that wraps the menu. You can use this identifier in JavaScript code to access the menu DOM or in functional tests to assert the menu items.");
    obj.setMandatory("0");
    obj.setName("id");
});
